name: Build

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Build app
        run: ./build.sh release zip
      - uses: actions/upload-artifact@v4
        with:
          name: Perch
          path: build/Perch.zip
